"use client";

// This file is intentionally left blank.
// The functionality for parsing SLD styles has been removed in favor of
// using the WMS GetLegendGraphic operation, which is simpler and more reliable
// as it does not require special authentication or complex parsing.
// The logic for displaying legends is now handled within LayerItem.tsx.
